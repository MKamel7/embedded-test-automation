name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    name: lint + types
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
      - name: Lint (ruff)
        run: uv run --group dev ruff check .
      - name: Type check (mypy strict)
        run: uv run --group dev mypy

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: ${{ matrix.python-version }}
      # socat provides the PTY pair the serial HIL tests run over. Installed
      # explicitly rather than relied on: those tests skip when it is absent,
      # so without this step the suite would go green having quietly stopped
      # exercising the pyserial path. `test_ci_installs_socat` asserts this
      # step exists, because the skip is only acceptable while something
      # guarantees the tests really run here.
      - name: Install socat (virtual serial port for the HIL tests)
        run: sudo apt-get update && sudo apt-get install -y socat
      # The coverage gate is set here rather than in pyproject so that running
      # a subset of the suite locally does not fail on partial coverage. 100%
      # is the real figure for the DUT and testbench, so anything less is a
      # regression, not a target.
      - name: Run test suite with coverage gate and HTML report
        run: >
          uv run --group dev pytest
          --cov-fail-under=100
          --cov-report=xml
          --html=report-py${{ matrix.python-version }}.html --self-contained-html
          --junitxml=junit-py${{ matrix.python-version }}.xml
      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: test-report-py${{ matrix.python-version }}
          path: |
            report-py${{ matrix.python-version }}.html
            junit-py${{ matrix.python-version }}.xml
            coverage.xml
